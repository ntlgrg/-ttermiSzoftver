<UserControl x:Class="ÉttermiSzoftver.ucRecept"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ÉttermiSzoftver"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid Background="#FF8F6D11">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="dgReceptek"  CanUserResizeColumns="True" CanUserSortColumns="True" CanUserAddRows="False" AutoGenerateColumns="False" Background="#FF8F6D11" Margin="0,1,0,-1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Megnevezés" Width="100" />
                <DataGridTextColumn Header="Recept" Width="200" />
            </DataGrid.Columns>
        </DataGrid>
        <Image x:Name="imKep" Grid.Column="1"  Stretch="Uniform" Margin="5,10,5,150"/>
    </Grid>
</UserControl>
